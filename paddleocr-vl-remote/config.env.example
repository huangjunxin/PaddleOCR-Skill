# paddleocr-vl-remote 个人配置（复制本文件为 config.env 后填写）
# 优先级: 命令行参数 > 环境变量 > 本文件 > 内置默认
#
# 服务地址（必须指向 OpenAI 兼容端点，以 /v1 结尾）
# PADDLEOCR_VL_URL=http://localhost:8111/v1
#
# 模型 id（须与服务端加载的模型一致）
# PADDLEOCR_VL_MODEL=PaddlePaddle/PaddleOCR-VL-1.6
#
# 若服务端以 --api-key 启动，填同名密钥
# PADDLEOCR_VL_API_KEY=
